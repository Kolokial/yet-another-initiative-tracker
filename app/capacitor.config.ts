import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.yetanotherinitiativetracker.app',
  appName: 'Yet Another Initiative Tracker',
  webDir: 'dist/app/browser'
};

export default config;
